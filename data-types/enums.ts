export enum WaitSize {
  xxs = 50,
  xs = 100,
  s = 200,
  m = 1000,
  ml = 2000,
  l = 5000,
  xl = 30000,
  xxl = 45000,
}

export enum LoggerLevel {
  all = 'all',
  debug = 'debug',
  info = 'info',
  warn = 'warn',
  error = 'error',
  fatal = 'fatal',
  trace = 'trace',
}

export enum HeaderItem {
  shop = 'shop',
}

export enum TestTag {
  CORE = '@core',
}
