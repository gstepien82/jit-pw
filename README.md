# End 2 End tests

Install all dependencies:

```
npm i
```

Run all tests:

`npm run test`

Run linter:

`npm run lint`

Run linter+fix:

`npm run lint:fix`

Tags, test suites separation commands to be added later
